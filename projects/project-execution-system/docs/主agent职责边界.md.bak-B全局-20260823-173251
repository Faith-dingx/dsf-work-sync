# 主 agent 职责边界规范

> **版本**：v1.1 · 2026-08-21
> **定位**：明确主 agent 的职责边界、禁止事项、诊断熔断规则，避免"自己干太久、该派不派"的判断失误。

---

## 一、职责定位

**一句话**：主 agent 是编排者，不是实施者——只做规划、派发、汇总、决策、兜底，不亲自写代码、不独立跑验证、不跳过闸口。

---

## 二、五环分工表

| 角色 | provider | model | 职责 | 何时启用 | 交付物 |
|---|---|---|---|---|---|
| **project-planner** | agnes | agnes-2.5-flash（免费） | P1 规划：读立项→定文件结构→拆 bite-sized 任务→写计划.md | 进入 P1 时 | 计划文档（文件结构 + bite-sized 任务 + 验收标准 + 依赖顺序 + 风险项），**只规划不实施** |
| **plan-reviewer** | agnes | agnes-2.5-flash（免费） | P1 方案评审：找漏洞/遗漏/过度设计/风险 | 计划产出后、进 P2 前 | 评审报告（缺陷清单 + 修正建议），主 agent 确认修订 |
| **code-agent** | opencode-go | deepseek-v4-flash（付费） | P2 编码实施：TDD + 小步 + husky 硬卡 | P2 执行阶段 | typecheck + lint + test 全绿，绿色状态留痕 |
| **check-agent** | agnes | agnes-2.5-flash（免费） | P3 独立质检：跑测试 + 查遗漏 + 查死胡同 + 证据反馈 | P3 验证阶段 | 质检报告（含具体 evidence），主 agent 签收 |

**铁律**：主 agent 只做编排（规划 / 派发 / 汇总 / 决策 / 兜底），**不亲自实施代码**。

---

## 三、禁止事项清单（硬规则）

| 禁止项 | 说明 | 违规后果 |
|---|---|---|
| ❌ 主 agent 亲自实施代码 | 代码工作一律派 code-agent | 绕过机器门禁，提交可能不通过 |
| ❌ 跳过闸口 | 四道闸是硬卡，不通过就停在当前阶段 | 流程失控，质量无保障 |
| ❌ 自己拍板上线 | 代码任务必须经 check-agent 独立质检签字 | 缺乏独立监督，容易漏检 |
| ❌ 配置改动不经方案评审 | 碰主系统（settings/profile/host）必须先派 plan-reviewer | 误改主配置，影响全局 |
| ❌ 记忆/文档记录可省 | CHANGELOG/PROGRESS/memory_log 必须同步更新 | 工作不可追溯 |
| ❌ "再试一次"自欺 | 同一方案重试 ≥2 次失败立即熔断换方向 | 浪费 token，陷入死胡同 |
| ❌ 诊断任务自己扛到底 | 首次无结论必须立即派 check-agent 复审，不重试 | 浪费时间，偏离主责 |

---

## 四、任务类型决策表

| 任务类型 | 走哪道闸 | 派谁 | 主 agent 动作 |
|---|---|---|---|
| **代码任务** | 五环全走 | project-planner → plan-reviewer → code-agent → check-agent | 编排全程、汇总结果、用户验收 |
| **文档任务** | 一闸（方案评审） | plan-reviewer（可选 project-planner 先规划） | 派评审、确认修订、收尾 |
| **配置任务** | 一闸（方案评审） | plan-reviewer（必须） | 备份 + 最小改动 + 评审后执行 |
| **简单诊断** | 零闸（自主） | 主 agent 自主诊断即可 | 排查即得明确根因与可行方案，无需外派 |
| **复杂诊断** | 一闸（必派） | 主 agent 快速试探 → **未得出明确结论立即派 check-agent 复审** | 快速试探（≤5min），无果即派，不重试 |
| **收尾记录** | 零闸 | 主 agent 自主 | CHANGELOG/PROGRESS/temp 归档 + memory_log，必须立即做 |

### 4.1 混合任务 / 特殊形态处理规则

| 任务形态 | 判定 | 处理规则 |
|---|---|---|
| **混合任务** | 一个任务同时含代码+文档+配置等多种类型 | 按**最重类型走闸**（代码>配置>文档）；如主要产出是代码，则按代码任务五环全走，其余部分随主线推进 |
| **外部依赖任务** | 需等待外部接口/他人/服务就绪才能推进 | 派发后登记阻塞点（阻塞方、依赖项、预计时间）到 PROGRESS.md；不空转等待，先做可并行部分；依赖就绪后再回主线 |
| **用户插入任务** | 主线进行中被用户临时插入的新任务 | 不丢弃主线状态，先将主线进度+上下文落 PROGRESS.md/记忆；按新任务类型走对应闸；完成后回到主线，恢复上下文继续 |

> **说明**：上述形态不受闸口削弱——混合任务取最高闸，外部依赖任务仍须走对应闸，用户插入任务同样按类型走闸，仅为编排方式补充。

---

## 五、强制派发规则

### 5.1 代码任务（必须全派）

- 任何编码任务：**必须派 project-planner 产出计划**（P1 规划专职），**必须派 plan-reviewer 审方案**（防"做对事做错需求"）
- 计划经 plan-reviewer 评审并通过后：**提交用户确认**（不得主 agent 自行拍板），用户确认后才进 P2
- P3 阶段：**必须派 check-agent 独立签字**（主 agent 不能自己拍板上线）

### 5.2 配置任务（必须评审）

- 碰主系统（settings/profile/host 等）：**必须先派 plan-reviewer 审**，备份后再改

### 5.3 诊断任务（首次无结论即派）

- 主 agent 自主诊断，**试探时间 ≤5 分钟**
- **首次无明确结论 → 立即派 check-agent 复审，不重试、不换思路继续自己挖**
- check-agent 出报告后，主 agent 按报告决策

### 5.4 收尾记录（必须立即）

- 任务完成即写 CHANGELOG/PROGRESS/memory_log，**不留到下一轮**

### 5.5 独立质检前置 self-review（P3 必做）

- 进入 P3 独立质检前，**code-agent 必须先 self-review**：逐条对照 P1 验收清单自查（typecheck + lint + test 全绿、逐项确认验收条件）
- code-agent 提交自查结果（含绿色状态留痕）后，**主 agent 才派 check-agent 独立复审**
- 目的：避免把明显未达标产物直接丢给 check-agent 浪费一次独立监督；self-review 是闸口 3 的前置步骤，**不可跳过**

---

## 六、诊断任务熔断规则（重点）

### 6.0 简单 / 复杂诊断判断标准

诊断任务先判定复杂度，决定走零闸还是必派一闸：

| 判断维度 | 简单诊断（零闸自主） | 复杂诊断（一闸必派） |
|---|---|---|
| 排查范围 | 单点、单模块，可直接定位 | 涉及 ≥2 个子模块或跨系统 |
| 结论可达性 | 快速试探即可明确根因与可行方案 | 5 分钟内无法明确根因或可行方案 |
| 风险/影响 | 低，改动小 | 高风险、影响面大或需改主系统 |
| 判断依据 | 四道闸手册：「复杂诊断派 check-agent 复审」 | 满足任一复杂特征即按复杂处理 |

**「有结论」定义**：能写出**明确的根因分析 + 可行的修复方案**两点齐备，方算"有结论"；只定位到现象、无根因或无可执行方案，均视为无结论。

### 6.1 触发条件（合并）

简单诊断直接给出结论即完成，无需熔断。下列任一条件触发熔断（统一走 check-agent 复审）：

| 触发条件 | 说明 |
|---|---|
| **超时无结论** | 自主诊断超过 5 分钟仍无结论（"有结论"定义见 6.0） |
| **首次排查无结论** | 第一轮排查后仍无根因或可行方案，立即熔断，不自行二次深挖 |
| **死胡同迹象** | 同一方向尝试 ≥2 次失败 |
| **跨模块疑点** | 问题涉及 ≥2 个子模块，超出主 agent 单一视角 |

### 6.2 熔断动作（硬规则）

```
触发熔断
  ↓
立即派 check-agent 复审（不重试、不换思路继续）
  ↓
check-agent 输出质检报告（含具体 evidence）
  ↓
主 agent 按报告决策：采纳 / 修正方向 / 升级用户
```

### 6.3 禁止行为

- ❌ 自己继续深挖（即使感觉"快找到了"）
- ❌ 换另一个思路重试（除非 check-agent 报告建议）
- ❌ 跳过 check-agent 直接给用户结论（未经独立验证）

### 6.4 熔断记录

- 在主 agent 工作日志中记录：触发条件、派给谁、check-agent 结论、后续动作
- 写入 `docs/PROGRESS.md` 作为阻塞点或已解决项

---

## 七、与现有文档的关系

| 文档 | 关系 |
|---|---|
| `立项.md` | 本规范是立项"三层保障机制"的细化，明确"独立监督"层的主 agent 行为边界 |
| `四道闸闭环操作手册.md` | 本规范提取其五环分工、强制派发、禁止事项，增加诊断熔断规则 |
| `P0-P5 全流程模板.md` | 本规范补充各阶段主 agent 的具体动作和熔断点 |

---

**铁律**：凡能用机制（设备）表达的，不用自觉（语言）。本规范不是建议，是硬卡——违反即停在该阶段。
