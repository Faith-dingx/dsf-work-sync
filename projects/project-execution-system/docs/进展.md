# 进展：项目全流程执行保障系统

> 系统目录：`/home/dingx/DSF-work/projects/project-execution-system/`
> 状态：**五环闭环全通**（project-planner / plan-reviewer / code-agent / check-agent 全实测跑通）；gpt-oss-20b 待 groq 限流放宽后回归

## 已完成

- [x] **M0 系统立项**（docs/立项.md）：背景/目标/范围/核心设计/验收/里程碑
  - 全流程 P0-P5（含 Superpowers 借鉴 7 点）
  - 三层保障机制（机器门禁 / 系统注入 / 独立监督）
  - "语言→设备"哲学
  - 4 子系统架构（能力路由 / 双 Agent / 质量门禁 / 流程模板）
- [x] 原 agent-capability-system 定位确认：= 子系统 1+2 的详细设计，并入本系统
- [x] 已有基础盘点：husky 门禁栈 = 子系统 3 基础；记忆/todo/goal/subagent/lsp = 底层支撑

## 设计机制（用户提出 2026-08-19）

- **换模型查盲区**：模型能力不足时看不到自己写的缺陷盲区；换一个不同模型（不一定要世界顶级）就可能找出关键故障点。→ 落地：代码卡死时，用不同模型（Grok/GPT/GLM 等）复审故障点（双 Agent 的监督质检可承载）。以后尝试。
- **兜底模型梯队（已调整 2026-08-19）**：L1 deepseek-v4-flash（0.037/0.11，最省）→ **L2 GPT-5.6-Luna（0.033/0.20，替代 pro——input 更便宜 + OpenAI 顶级代码模型，全面优于 pro 0.11/0.33）** → 卡死兜底 Grok-4.5 / GLM-5.3（go 订阅额度折算价，比原厂便宜 3-4 倍）。

## M1 完成：2 个 Agent Preset 建立（2026-08-19）

- **code-agent**（~/.dsh/.agent-presets/code-agent/）：代码实施，保留 fs/bash/lsp/todo/goal/planning/compaction/subagent；去掉 cordis/workflow/ralph/管理。persona 硬性规则：TDD、小步、大文件禁全读、husky 门禁、死胡同熔断。
- **check-agent**（~/.dsh/.agent-presets/check-agent/）：监督质检，只读+跑测试，查流程/遗漏/死胡同/全局/质量，证据先行；去 web/goal/planning/subagent/cordis。
- 两者用全局 superpowers skill（tool-skill 自动加载）；preset.yml 带 name/description。
- **验证**：cordis 动态插件调 agentPresets.standingKeyFor 挂载验证通过（2 个均 MOUNTED OK）。
- **遗留**：code-agent 的 go 模型（deepseek-v4-flash/GPT-5.6-Luna）需在 settings/9888 配置，模型在会话启动时选择。

## M1 补充：OpenCode Go 官方 API 接入成功（2026-08-19）

- **端点**：https://opencode.ai/zen/go/v1（OpenAI 兼容），key = OPENCODE_GO_API_KEY（credentials 已配，实测调通）
- **模型**：官方 28 个（deepseek-v4-flash/pro、gpt-5.6-luna、grok-4.5、glm-5.2/5.3、kimi-k2.7-code 等）；deepseek-v4-flash 实测 chat 200（1.9s）
- **9888 网关澄清**：仅提供 7 个免费模型（agnes/qwen 等）→ check-agent 用；go 付费模型走官方 API → code-agent 用
- **settings 已配置**：llm-pi-ai/providers/opencode-go → 官方 baseURL + 28 模型（需重启 dsh 生效）

## 职责定位固化（用户约定 2026-08-19）

- **主 agent = 编排者**：规划/任务分解/派发/汇总/决策/兜底（换模型查盲区），**不亲自实施代码**
- **code-agent = 实施**：接收派发的编码任务，TDD/流程执行
- **check-agent = 质检**：独立监督，证据反馈
- 协作闭环：主规划 → code 实施 → check 质检 → code 修正 → 主汇总
- 落地方式待定：主 agent 派生子 agent 继承 preset（同模型）；独立 preset 需独立会话（独立模型 go/agnes）——M2 设计协作流时明确

## M2 双 Agent 协作流：落地方式定案 + 进入实施（2026-08-20）

### 落地方式定案（用户确认）

- **独立 preset + 独立会话 + 独立模型**：code-agent（go 付费）/ check-agent（agnes 免费）
- 但日常全自动为主：主 agent spawn 子代理，经「按 preset 组合」改造后跑真 preset（精确工具+硬规则+独立模型），**无需手动开会话**
- 独立 preset 独立会话降级为可选重型保障通道；Agent Teams（rc.8 官方编排层）留作后续可选增强

### 关键决策记录（调研过程）

- 子代理现状：永远继承父 preset（child-agent.ts applyChildComposition 写死 composeFrom parentCtx），工具杂、规则靠 prompt
- 改造可行性探查确认：agentPresets.standingKeyFor(presetId) 预挂载 + bindScopeParent 公开 API + 同步/异步边界（async 阶段 standingKeyFor → setup 传 standingKey）
- 方案文档：docs/M2-子代理preset改造方案.md（6 源码文件 + 测试 + 文档，向后兼容）
- 方案已确认，2026-08-20 派 code-agent 实施（后台），完成后 check-agent 审核 → 首次完整「主编排→码实施→检审核」闭环实战

## 待办

- [ ] M1 全流程定义 + 流程模板（子系统 4）
- [ ] M2 能力路由 skill-router（子系统 1）
- [ ] M3 双 Agent（子系统 2）
- [ ] M4 质量门禁整合（子系统 3）
- [ ] M5 端到端验证

## 阻塞

- 无

### M2 实施完成（2026-08-20 20:3x）

- **提交**：deepseek-harness `4b67817a48`（feat(subagent): support composing children from a named agent preset），15 文件 +371/-17 + 3 fixture
- **闭环实战**：主agent编排 → code-agent 实施（TDD/803测试/typecheck/lint 全绿）→ check-agent 独立审核（建议提交）→ code-agent 补审核建议测试 → 提交
- **能力落地**：call_code_agent/check-agent 可指定 presetId 跑真 code-agent/check-agent preset（精确工具+硬规则+独立模型）
- **待办**：Step 5 配置层（full preset 加 presetId）+ 重启 dsh + GUI 实测

### plan-reviewer 方案评审 Agent + Step5 配置完成（2026-08-20 20:4x）

- **新增 plan-reviewer**：实施前方案评审（挑缺陷/漏洞/遗漏/过度设计，提合理化建议），模型 gpt-oss-20b（9888 免费，实测选型胜出），只读工具集，standingKeyFor 真挂载验证通过
- **闭环升级为四道闸**：方案评审 → 实施 → 质检 → 提交（补上防"做对事做错需求"的一环）
- **Step5 配置完成**：full preset 三子代理工具全部加 presetId（code/check/plan-reviewer），等重启 dsh 一次生效

### M2 落地遇阻与修复：plan-reviewer 400 → 换 agnes（2026-08-20 21:5x）

- **现象**：重启后实测 call_plan_reviewer（router-9888/gpt-oss-20b）首轮模型请求 400 "Invalid JSON body"，4 次子代理全失败；code/check（opencode-go/agnes）正常
- **诊断链**：presetId 机制正常（code/check 真 preset 验证通过、plan-reviewer 会话 header 正确）→ 模型解析正常 → dsh 内部 llm.stream 各种复现全 200 → 9888 网关硬限制（~30KB 请求体 → 413、groq 限流 429 频繁）
- **结论**：router-9888 软路由网关对 dsh 子代理大请求不兼容（400/413/429），非 dsh 问题
- **修复**：plan-reviewer 改 agnes/agnes-2.5-flash（备份 .bak-plan-reviewer-agnes-213749）→ 重启后实测通过，**四道闸闭环全通**
- **待办**：等用户提供软路由系统代码 → 精确定位 400 → 出修改方案 → gpt-oss-20b 回归

## M1 补全：P0-P5 全流程模板落地（2026-08-20）

- **完成**：docs/P0-P5全流程模板.md 产出，P0（立项）~P5（收尾）每阶段含目标/清单/产出物/闸口，三层保障机制嵌入说明，四道闸硬卡规则
- **系统完整度**：4 子系统（能力路由/双Agent/质量门禁/流程模板）+ 三层保障机制 + 四道闸闭环，立项设计中所有组件均已落地或已跑通实测
- **当前阻塞**：router-9888 软路由网关对 dsh 子代理大请求 400（影响 plan-reviewer gpt-oss-20b 回归），需用户提供软路由代码定位修复；当前 plan-reviewer 用 agnes-2.5-flash 顶上，四道闸闭环正常

## 五环升级：project-planner 项目规划师建立（2026-08-20 23:5x）

- **新增 project-planner**（~/.dsh/.agent-presets/project-planner/，agnes-2.5-flash）：P1 规划专职——读立项→定文件结构→拆 bite-sized 任务→每任务验收标准→写 docs/计划.md，只规划不实施
- **闭环升级为五环**：project-planner(规划) → plan-reviewer(评审) → code-agent(实施) → check-agent(质检) → 提交（补上"规划独立"一环，规划与评审分离）
- **实测**：call_project_planner 产出合格计划（换模型查盲区功能规划，5 bite-sized 任务+验收+依赖+风险）
- 操作手册已升级五环；full preset 挂载 call_project_planner
