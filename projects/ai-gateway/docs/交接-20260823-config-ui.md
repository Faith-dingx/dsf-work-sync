# 交接记录 2026-08-23（新对话接手用）

> 目的：历史对话过大，用户新开会话。本文件承载"现在在哪、下一步做什么、关键命令/文件"。

## 一、当前主任务线（唯一活跃）：config-ui（AI 网关配置界面）本地闭环

- 目标（用户最高指示）：**先解决本地 config-ui 与原厂联动并验收通过；未验收前不谈打包/软路由**。
- 服务：`config-ui` = systemd 用户服务（`~/.config/systemd/user/config-ui.service`），0.0.0.0:3083，`Linger=yes`。
- 访问：`http://10.10.10.9:3083/`，token=`cat /home/dingx/.dsh/.config-ui-token`
- 代码：`/home/dingx/DSF-work/projects/ai-gateway/config-ui/`（server.js=后端+静态托管；index.html/app.js/style.css=前端；原生无框架）
- 数据源（真实原厂）：`CKD_CONFIG_DIR=/home/dingx/.dsh`（settings.yaml）、`PRESETS_DIR=/home/dingx/.dsh/.agent-presets`

## 二、已完成（本轮收敛）

1. 端口定案：**3081=dsh 完整主入口（主用）；3080=受限对外；3083=config-ui；3082/3084-3089 预留**——见 `docs/3081完整入口-专项记录.md`（双监听/围栏差异/失效排查预案 5 步）
2. config-ui 已与原厂联动：读真实 settings.yaml（5 provider）、真实预设（5 个）、模型下拉 68 个原厂模型
3. 三处修复（subagent-28 完成，**未 commit、真实配置零改动**）：
   - 子代理模型**多源解析**：code=opencode-go-2/deepseek-v4-flash / code2=opencode-go-2/deepseek-v4-pro / check·plan-reviewer·project-planner=agnes/agnes-2.5-flash（来源徽标：挂载层/preset/继承默认）
   - 子代理**编辑走挂载层**（full/agent.cordis.yml 的 tool-subagent-<id> 块；无字段自动新增；备份+原子写+写前 YAML 校验+失败回滚）；无挂载块→404+提示（走默认或"＋添加"自定义）
   - 密钥页：枚举 settings 的 apiKeyEnv（AGNES_API_KEY/HUOSHAN_API_KEY/OPENCODE_GO_API_KEY/OPENCODE_GO_API_KEY_2）→ 环境变量来源 + 已配置/未配置（AGNES 已配置，其余未配置=真实状态）；不再误导"空"
4. systemd 托管根治 3083 被会话回收问题

## 三、立即待办（用户操作 + 验收）

1. **重启 config-ui 加载新代码**（用户专属，命令见本文件顶部）：
   `export XDG_RUNTIME_DIR=/run/user/$(id -u)` 后 `systemctl --user restart config-ui`
2. 刷新 `http://10.10.10.9:3083/` 验收三处（子代理真实模型+来源、可编辑、密钥状态）；有意见继续改，**验收通过前不谈下一步**

## 四、挂起项（用户明确"后置/暂缓"）

- **v0.2 安装包/软路由打包**：暂停（用户：先本地闭环）
- **三方通话/dsh 对外对话端口（API）**：想法池 `docs/AI网关-想法待办.md` ① 号，后置
- 软路由侧：config-ui 密钥将用 dsh.env（环境变量方案），**本地版不引入 dsh.env**（用户强调本地优先）
- 软路由 Hermes 凭据（U1-U3）、云盘备份上传、GitHub 后续（npm 发布/PR 官方）均在待办

## 五、关键文件/命令速查

- 服务：`systemctl --user {status|restart|stop} config-ui`（先 export XDG_RUNTIME_DIR）
- 日志：`journalctl --user -u config-ui -f`
- 文档：`docs/3081完整入口-专项记录.md`、`docs/AI网关-想法待办.md`、`projects/ai-gateway/docs/计划-配置Web界面.md`、`docs/软路由资产与热备方案.md`、`docs/CHANGELOG.md`
- 代码工作树：deepseek-harness（feature/lan-access，GitHub fork 已备份，未 push 新提交）
- **记忆纪律**：主 agent 不直接改 WORKSPACE.md（guard 白名单外）；docs/ 可写；长文档用"主 agent 大纲+code-agent 直写"
