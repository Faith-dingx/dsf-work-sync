# dsh 迁入软路由检查清单（迁移专用）

> 日期：2026-08-25 · 维护：main-agent（guarded）
> 用途：dsh 从本机（10.10.10.9）迁入软路由（10.10.10.2）时的逐项核对表。迁移进行中，随进度更新。

## 一、部署不变量（迁移时禁止改动）

| 项           | 值                                                                                 | 说明                                                                                                                                               |
| ------------ | ---------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| 模型路由地址 | `http://10.10.10.2:9888/...`（guard L271/278、skill-router L263、compaction L146） | **刻意保留 IP 不归一 localhost**：软路由是局域网 AI 网关固定服务端，任何设备 clone 配置即用。dsh 迁入后自身即 10.10.10.2，局域网 IP 访问自身可达。 |
| 网关其他端口 | :7000 面板 / :8081 监控 / :9119 Hermes / :80 LuCI / :7681 ttyd / :5244 AList       | 软路由资产，随盘走                                                                                                                                 |

## 二、必须随迁的数据目录（主 agent 记忆中枢）

- [ ] `DSF-work/`（工作区 + docs/CHANGELOG + 记忆中枢 + projects/）
- [ ] `~/.dsh/.agent-presets/`（full/子代理 persona + 36 备份）
- [ ] `~/.dsh/memory/`（用户级 MEMORY.md + 工作区记忆/日志/反思）
- [ ] `~/.dsh/sessions/`（299M 会话存档，FTS 检索依赖）
- [ ] `~/.dsh/.credentials.yaml`（密钥，chmod 600）
- [ ] `~/.dsh/settings.yaml`（provider 路由、preset 选择）
- [ ] `~/.dsh-memory/`（若存在）

## 三、harness 版本决策

- [ ] 确认迁移载体：带 `feature/lan-access` 分支（24 个本地 commit：LAN 访问改造 + guard 三件套 + 工具面收敛）还是升级 origin/master（本地落后 743 commit）
- [ ] 建议：先带 feature/lan-access 过去，迁完稳定后再评估升级（升级顺带解决 B1 会话噪音等 harness 本体问题）

## 四、迁移后验证（按四道闸精神逐项）

- [ ] dsh-web 启动 + Web UI 首屏（3080）
- [ ] 主 agent 工具清单：cordis_inspect_* 在、define/run 不在（allowMutation=false）
- [ ] guard：rm 硬禁生效、只读命令放行、文件白名单路径（DSF-work/docs/... 需与新机器路径一致）
- [ ] 9888 可达（10.10.10.2 上自身访问）
- [ ] 记忆注入/压缩/摘要正常
- [ ] 子代理五个固化工具可派发（models 与文档 v1.3 对齐）

## 五、远期架构（全部设备资料信息共享）

- [ ] 各设备 dsh 实例统一指向 10.10.10.2:9888（配置已天然支持，clone 即用）
- [ ] 会话/记忆多设备共享方案（待设计：同步 or 网关集中存储）
- [ ] config-ui 作为网关统一管理面（已具备回滚/密钥/模型管理能力）

## 六、迁移时顺手处理的自身问题

- [ ] B1 会话噪音（runtime context 快照重复注入/skill catalog 重复推送）——harness 本体，升级时复查
- [ ] B2 文档×配置对账脚本（进 lefthook）——独立增值，迁移前后均可做
- [ ] DSF-work git 归档提交（47 项未提交）
