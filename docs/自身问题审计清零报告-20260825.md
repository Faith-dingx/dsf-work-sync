# dsh 自身问题审计·清零报告

> 日期：2026-08-25 · 审计：4 个 check-agent 分批(基础设施/安全面/配置对账/记忆会话)+ 主 agent 复核
> 状态：清零执行中(转派 code-agent subagent-8)

## 一、审计结论(批次汇总)

### 健康项(无需处理)

- 服务运行正常(dsh-web active、config-ui active、9888 网关 200)
- 安全面无 H/S 漏洞:rm 硬禁 4 通道(76 条真实拦截)、白名单 realpath 防逃逸(320 条拦截)、cordis 只读收口、凭据 600
- 依赖完整、git 干净、无插件加载失败、记忆体系无 pending-suggestions 复发
- ELIFECYCLE"崩溃"= 用户有意重启的退出打印(审计误判已修正);开机自启 enabled(误判已修正)

### 问题项(清零清单)

| #   | 级别 | 问题                             | 根因                                                                               | 处理                                  |
| --- | ---- | -------------------------------- | ---------------------------------------------------------------------------------- | ------------------------------------- |
| 1   | H    | journald 4.1G 膨胀               | {{commit}} 噪音+历史累积                                                           | vacuum 200-500M + 修噪音源            |
| 2   | H    | `{{commit}}` 每天 273 条日志警告 | failures.md/备份含裸字面量                                                         | 替换为反引号包裹(存根因链)            |
| 3   | S    | 白名单交互目录路径错误           | 指向不存在的 /home/dingx/与agent的交互目录/                                        | 修正为 DSF-work 下真实路径            |
| 4   | S    | sessions 301M(query.sqlite 116M) | 历史会话累积                                                                       | >30 天旧会话 zstd 归档,检索库不动     |
| 5   | M    | .temp 367M                       | 临时文件长期滞留                                                                   | 有保留归档/无价值删/拿不准留          |
| 6   | L    | .temp/node_modules 残留 symlink  | 归档遗留                                                                           | rm -f 只删链接本体                    |
| 7   | L    | memory 目录 664                  | 初始权限                                                                           | 保留(内容无密钥),登记                 |
| 8   | ⏸    | restoreLastAgent 启动循环 8min   | **外部插件 @a9i5k4/dsh-auto-memory 时序**(agent 服务注册晚于插件加载,15s 心跳轮询) | 待决策(本地 patch 易失/接受/上报作者) |
| 9   | ⏸    | B1 会话噪音(上下文重复注入)      | harness 本体                                                                       | 迁移升级时复查                        |

## 二、对话/工作区整理

- 会话存档:旧会话归档(任务 D)
- 记忆纪律已入册:清理删除前必须审核(清单+理由+波及分析),已写入用户级记忆
- 项目笔记已精简(9 条 1465 字符)

## 三、清零执行状态

- code-agent subagent-8 执行 A-E(日志/{{commit}}/.temp/会话归档/白名单),完成后核对
- restoreLastAgent 与 B1 两项待用户决策/迁移时处理

## 四、后续

清零核对通过后 → 全量备份 → 迁移软路由(检查清单 docs/dsh迁入软路由检查清单.md)
